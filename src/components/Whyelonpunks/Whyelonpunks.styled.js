import styled from "styled-components";

const Wrapper = styled.div`
  .punks-my-card {
    .punks-card-img {
      width: 170px;
      height: 170px;
      border-radius: 50%;
      margin: auto;
      background: #1a1161;
      img {
        width: 100px;
      }
    }
  }
`;

export default Wrapper;
