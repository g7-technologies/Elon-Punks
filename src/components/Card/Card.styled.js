import styled from "styled-components";

const Wrapper = styled.div`
  .my-card {
    margin-top: 20px;
    padding: 15px;
    background: linear-gradient(158.02deg, #1a1161 -2.54%, #1c0674 85.62%);
    box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
    border-radius: 5px;
    .card-head {
    }
    .card-img {
      text-align: center;
      img {
        width: 150px;
        height: 150px;
      }
    }
  }
`;

export default Wrapper;
